# =============================================================================
# Scraper Cyberpunk TCG — dépendances
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# =============================================================================

# Client HTTP asynchrone (retries, timeouts, HTTP/2)
httpx==0.28.1

# Extraction HTML de la base de cartes
beautifulsoup4==4.13.4

# Modèle de carte typé + validation de sortie
pydantic==2.11.9

# Validation des exports contre docs/schemas/card.schema.json
jsonschema==4.25.1

# Tests
pytest==8.4.2
