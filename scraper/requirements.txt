# =============================================================================
# Scraper Cyberpunk TCG — dépendances
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# =============================================================================

# Client HTTP synchrone (retries, timeouts, cache local)
httpx==0.28.1

# Modèle de carte typé + validation de sortie
pydantic==2.11.9

# Validation des exports contre backend/src/main/resources/schema/card-schema.json
jsonschema==4.25.1

# Tests
pytest==8.4.2
